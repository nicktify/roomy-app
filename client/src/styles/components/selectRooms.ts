import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  root: {
    borderRadius: 40,
    width: 130,
    alignItems: 'center',
    padding: 10,
    margin: 20,
  },
  text: {
    color: 'white',
    fontWeight: 'bold'
  }
});

export default style;