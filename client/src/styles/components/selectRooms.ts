import { StyleSheet } from "react-native";

const style = StyleSheet.create({
  root: {
    borderRadius: 5,
    width: 130,
    alignItems: 'center',
    padding: 10,
  },
  text: {
    color: 'white',
    fontWeight: 'bold'
  }
});

export default style;